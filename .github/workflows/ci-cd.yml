name: CI/CD Pipeline

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
    - uses: actions/checkout@v4
    - uses: dorny/paths-filter@v3
      id: filter
      with:
        filters: |
          backend:
            - 'backend/**'
            - '.github/workflows/backend-ci.yml'
            - '.github/workflows/ci-cd.yml'
          frontend:
            - 'frontend/**'
            - '.github/workflows/frontend-ci.yml'
            - '.github/workflows/ci-cd.yml'

  backend:
    needs: changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    uses: ./.github/workflows/backend-ci.yml
    secrets: inherit

  frontend:
    needs: changes
    if: ${{ needs.changes.outputs.frontend == 'true' }}
    uses: ./.github/workflows/frontend-ci.yml
    secrets: inherit

  e2e:
    needs: [backend, frontend]
    if: ${{ always() && (needs.backend.result != 'failure' && needs.frontend.result != 'failure') }}
    uses: ./.github/workflows/e2e-test.yml
    secrets: inherit

  debug-checks:
    runs-on: ubuntu-latest
    needs: [backend, frontend, e2e]
    if: ${{ always() && (needs.backend.result != 'failure' && needs.frontend.result != 'failure' && needs.e2e.result != 'failure') }}
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install websockets
    
    - name: Run Quick Health Check
      run: |
        chmod +x ./scripts/debug/debug.sh
        ./scripts/debug/debug.sh quick --json > quick_check_report.json || true
    
    - name: Run Security Audit
      run: |
        ./scripts/debug/debug.sh security --json > security_report.json || true
    
    - name: Upload Debug Reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: debug-reports
        path: |
          quick_check_report.json
          security_report.json
        retention-days: 30

  coverage:
    runs-on: ubuntu-latest
    needs: [backend, frontend, e2e]
    if: ${{ always() && (needs.backend.result != 'failure' && needs.frontend.result != 'failure' && needs.e2e.result != 'failure') }}
    steps:
    - uses: actions/checkout@v4

    - name: Download backend coverage
      uses: actions/download-artifact@v4
      with:
        name: backend-coverage
        path: backend-coverage
      continue-on-error: true

    - name: Download frontend coverage
      uses: actions/download-artifact@v4
      with:
        name: frontend-coverage
        path: frontend-coverage
      continue-on-error: true

    - name: Upload combined coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        fail_ci_if_error: false
        verbose: true

  notify:
    runs-on: ubuntu-latest
    needs: [backend, frontend, e2e]
    if: ${{ always() }}
    steps:
    - name: Notify on success
      if: ${{ (needs.backend.result == 'success' || needs.backend.result == 'skipped') && (needs.frontend.result == 'success' || needs.frontend.result == 'skipped') && (needs.e2e.result == 'success' || needs.e2e.result == 'skipped') }}
      run: |
        echo "✅ All tests passed!"
        echo "Backend: ${{ needs.backend.result }}"
        echo "Frontend: ${{ needs.frontend.result }}"
        echo "E2E: ${{ needs.e2e.result }}"

    - name: Notify on failure
      if: ${{ needs.backend.result == 'failure' || needs.frontend.result == 'failure' || needs.e2e.result == 'failure' }}
      run: |
        echo "❌ Some tests failed!"
        echo "Backend: ${{ needs.backend.result }}"
        echo "Frontend: ${{ needs.frontend.result }}"
        echo "E2E: ${{ needs.e2e.result }}"
        exit 1
